jQuery(document).ready(function ($) {
  /**
   * Cart Dropdown Toggle
   * @type {*}
   */
  /*$('.cc-wc-cart-drop-widget .header-cart-wrap').click(function () {
      if ($(this).hasClass('active')) {
          $(this).data('open', 'false');
          $(this).removeClass('active');
      } else {
          $(this).data('open', 'true');
          $(this).addClass('active');
      }
  });
  $('body').bind('click', function(e){
      var cart_wrap = $('.cc-wc-cart-drop-widget .header-cart-wrap');
      if( cart_wrap.data('open') === 'true' && !$(e.target).is('.cc-wc-cart-drop-widget .header-cart-wrap') && !$(e.target).closest('.header-cart-wrap').length ){
          cart_wrap.data('open', 'false');
          cart_wrap.removeClass('active');
      }
  });*/

});// END document.ready